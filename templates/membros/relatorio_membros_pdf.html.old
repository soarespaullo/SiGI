<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Relatório de Membros</title>
  <style>
    body { font-family: Arial, sans-serif; font-size: 12px; }
    h2 { text-align: center; margin-bottom: 20px; }
    table { width: 100%; border-collapse: collapse; margin-top: 20px; }
    th, td { border: 1px solid #ccc; padding: 6px; text-align: left; }
    th { background: #f2f2f2; }
  </style>
</head>
<body>
  <h2>Relatório de Membros</h2>

  {% if sexo or status or estado_civil or funcao %}
    <p><strong>Filtros aplicados:</strong>
      {% if sexo %}Sexo: {{ sexo }}{% endif %}
      {% if status %} | Situação: {{ status }}{% endif %}
      {% if estado_civil %} | Estado Civil: {{ estado_civil }}{% endif %}
      {% if funcao %} | Tipo: {{ funcao }}{% endif %}
    </p>
  {% else %}
    <p><em>Nenhum filtro aplicado. Relatório completo.</em></p>
  {% endif %}

  <table>
    <thead>
      <tr>
        <th>Nome</th>
        <th>Sexo</th>
        <th>Status</th>
        <th>Estado Civil</th>
        <th>Função</th>
      </tr>
    </thead>
    <tbody>
      {% for m in membros %}
      <tr>
        <td>{{ m.nome }}</td>
        <td>{{ m.sexo or 'Não informado' }}</td>
        <td>{{ m.status or 'Não informado' }}</td>
        <td>{{ m.estado_civil or 'Não informado' }}</td>
        <td>{{ m.funcao or 'Não informado' }}</td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
</body>
</html>
